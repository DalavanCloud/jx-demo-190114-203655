apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/ascheman/jx-demo-190114-203655:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/ascheman/jx-demo-190114-203655:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-demo-190114-203655
        chartPath: charts/jx-demo-190114-203655
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/ascheman/jx-demo-190114-203655"
          image.tag: "{{.DIGEST_HEX}}"
